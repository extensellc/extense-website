---
/*
  /solutions/ai-ready-content

  Sub-page deep-dive on the AI-Ready Content solution area.

  Reframed to the old ex-tense.co page's SOLUTION-led sections only — its
  problem-education and ROI ($540K) blocks are dropped per the locked
  thesis. Solution-led order: claim → proof → reach → method → offer →
  cross-sell → convert.

  Sections:
    1. Hero — "Retrievable, by design." + precision-gauge SVG
    2. Siblings nav
    3. Two pipelines — before/after (the proof)
    4. Every AI use case needs the same foundation — 7-row table (the reach)
    5. From unstructured to AI-ready in 5 phases (the method)
    6. We Engineer — We Don't Just Advise (the offer)
    7. Explore Related Solutions (3-up)
    8. CTA-as-closer (+ accentLine)
*/

import BaseLayout from "../../layouts/BaseLayout.astro";
import AiReadyHero from "../../components/site/AiReadyHero.astro";
import SolutionsSiblings from "../../components/site/SolutionsSiblings.astro";
import AiReadyComparison from "../../components/site/AiReadyComparison.astro";
import AiReadyUseCases from "../../components/site/AiReadyUseCases.astro";
import AiReadyPipeline from "../../components/site/AiReadyPipeline.astro";
import AiReadyOffer from "../../components/site/AiReadyOffer.astro";
import AiReadyRelated from "../../components/site/AiReadyRelated.astro";
import CTAModule from "../../components/site/CTAModule.astro";
---

<BaseLayout
  title="AI-Ready Content"
  description="Retrievable by design. Typed DITA topics, metadata filters, and section-ID stability that turn 85% RAG precision into a measurable property of the corpus — and feed every AI system from one engineered source."
>
  <AiReadyHero />
  <SolutionsSiblings currentSlug="ai-ready-content" />
  <AiReadyComparison />
  <AiReadyUseCases />
  <AiReadyPipeline />
  <AiReadyOffer />
  <AiReadyRelated />
  <CTAModule
    audience="general"
    compact
    accentLine
    body="Send us 20 topics from your current documentation. We’ll score them on 7 AI-readiness dimensions and return a gap analysis with the next concrete steps — within two business days."
  />
</BaseLayout>
